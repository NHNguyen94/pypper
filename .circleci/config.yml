# This config was automatically generated from your source code
# Stacks detected: deps:python:.,package_manager:poetry:.
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-python:
    # Install dependencies and run tests
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout
#      # Set PYTHONPATH environment variable
#      - run:
#          name: Set PYTHONPATH
#          command: echo "export PYTHONPATH=." >> $BASH_ENV
#      # Activate Bash environment to make PYTHONPATH available
#      - run:
#          name: Activate Bash environment
#          command: source $BASH_ENV
      - python/install-packages:
          pkg-manager: poetry
      - run:
          name: Run tests
          command: PYTHONPATH=. poetry run pytest --junitxml=junit.xml || ((($? == 5)) && echo 'Did not find any tests to run.')
      - store_test_results:
          path: junit.xml
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
workflows:
  build-and-test:
    jobs:
      - test-python
    # - deploy:
    #     requires:
    #       - test-python
